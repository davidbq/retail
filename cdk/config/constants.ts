// General Constants
export const CUSTOMERS = 'customers';
export const SERVICE = 'service';
export const RETAIL = 'retail';

// Derived Constants
export const RETAILCUSTOMERS = RETAIL.charAt(0).toUpperCase() + CUSTOMERS.charAt(0).toUpperCase();